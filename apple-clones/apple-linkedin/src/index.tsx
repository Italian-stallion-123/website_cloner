import React from "react";
import ReactDOM from "react-dom/client";
import AppleLinkedIn from "./AppleLinkedIn";
import "./index.css";

const root = ReactDOM.createRoot(
  document.getElementById("root") as HTMLElement
);
root.render(
  <React.StrictMode>
    <AppleLinkedIn />
  </React.StrictMode>
);
